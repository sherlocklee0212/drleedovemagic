# ══ 不要上傳到 GitHub 的東西 ══════════════════════════════
# 這個檔案列出的項目，Git 都會自動忽略，不會被上傳。

# ── 金鑰與密碼（最重要）──────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.keystore
id_rsa
id_rsa.*
id_ed25519
id_ed25519.*
credentials.json
secrets.yml
secrets.yaml
.netrc

# ── 個人與機密資料 ──────────────────────────────────────
# 不想上傳的東西放進 private/ 資料夾就好。
# 裡面所有檔案都會被忽略，只有 README.txt 例外——
# 留著它，第二台電腦下載後才會有這個資料夾。
private/*
!private/README.txt
*.private.*

# ── Jekyll 建置產物 ─────────────────────────────────────
_site/
.jekyll-cache/
.jekyll-metadata
.sass-cache/

# ── 套件與相依檔案 ──────────────────────────────────────
Gemfile.lock
vendor/
vendor/bundle/
.bundle/
*.gem
node_modules/

# ── 系統暫存檔 ──────────────────────────────────────────
.DS_Store
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini
$RECYCLE.BIN/

# ── 編輯器設定 ──────────────────────────────────────────
.vscode/
.idea/
*.swp
*~

# ── 記錄與備份檔 ────────────────────────────────────────
*.log
*.bak
*.orig
*.tmp

# ── Claude Code 的本機設定（每台電腦不同，不該同步）──────
.claude/settings.local.json

# ── 本機保留、不上傳的原始檔 ────────────────────────────
# 站徽原圖（網站實際使用的是 assets/img/ 底下的版本）
flavicon.png
