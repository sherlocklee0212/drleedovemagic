# 給 Claude 的工作規則

## 溝通方式

**使用者不是工程師，一律用繁體中文溝通。**

解釋事情時避免技術術語；必須用到的時候，順手解釋那是什麼。
不要假設使用者知道 git、分支、部署這類概念。

---

## 每次開始工作時

1. 先執行 `git pull`，取得最新版本（使用者有兩台電腦，可能在另一台改過東西）。
2. 閱讀 `進度.md`，了解上次做到哪、做過哪些決定、下一步是什麼。

---

## 寫文章或修改文章之前

**先讀 `寫作風格.md`**，照裡面的節奏、用詞、內容來源規則來寫。

使用者每次修改文章的用詞或內容，就把背後的原則補進 `寫作風格.md`，
讓下一篇文章不會再犯同樣的問題。

---

## 當使用者說「今天結束」時

依序完成這三件事：

1. **更新 `進度.md`**：寫進今天的進度、做過的決定、下一步待辦，並更新最後更新日期。
2. **commit**：訊息用繁體中文，說清楚改了什麼。
3. **push 到 GitHub**。

完成後告訴使用者推上去了，以及這次更新了哪些內容。

---

## 安全規則

**不要把個人資料、密碼、金鑰放進 repo。**

- 金鑰、token、密碼一律放 `.env`，該檔案已被 `.gitignore` 排除。
- 不想上傳的東西放 `private/` 資料夾，同樣已被排除。
- 執行 `git add` 之前先確認加入了什麼，避免把不該上傳的檔案一起送上去。
- 這個 repo 是**公開**的，任何人都看得到內容與完整歷史。

---

## 這個專案是什麼

「李醫師的鳩是魔法」（DrLeeDoveMagic）——白斑鳩的飼育紀錄與鴿子魔術表演網站。

- 網站：https://drleedovemagic.net
- 技術：Jekyll + GitHub Pages，推上 `main` 分支後約一兩分鐘自動發佈
- 文章放 `_posts/`，檔名格式 `年-月-日-英文代號.md`
- `_drafts/` 裡的檔案不會發佈，可以放範本或草稿

詳細的檔案結構與寫作方式見 `README.md`，專案脈絡見 `進度.md`。

---

## 這個 repo 必須維持公開

GitHub 免費方案規定：private repo 無法發佈 GitHub Pages。
改成私人會讓 `drleedovemagic.net` 立刻離線。

需要保密的內容不要放進這個 repo，放 `private/` 或另外開一個私人 repo。
