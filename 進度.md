# 進度紀錄

這個檔案記錄網站的進度、已經做過的決定、還有下一步要做什麼。
每次開始工作前先看這裡，就知道上次做到哪。

**最後更新：2026-09-20**

---

## 這是什麼專案

「**李醫師的鳩是魔法**」（DrLeeDoveMagic）——記錄白斑鳩的日常飼育、育雛、訓練、
健康照護，以及鴿子魔術的表演紀錄。

- **網站**：https://drleedovemagic.net
- **原始碼**：https://github.com/sherlocklee0212/drleedovemagic
- **技術**：Jekyll + GitHub Pages（推上 GitHub 就自動發佈，不需要本機安裝任何東西）

---

## 目前進度

### 已完成

**網站架構**
- 首頁、關於李醫師、全部文章、404 頁
- 六大分類：鳩日常（daily）、鳩來了（hatchling）、鳩上手（training）、
  鳩診室（health）、鳩知道（knowledge）、鳩是魔法（magic）
- 文章版型、分類版型、RSS 訂閱、sitemap

**視覺**
- 配色：紙白底 × 鳩羽色（帶灰的紫），支援深色模式
- 首屏：舞台照當主視覺，深色背景與照片黑底融合
- 站徽：白斑鳩與聽診器，用於頁首、頁尾、瀏覽器分頁圖示

**網域與上線**
- 網域 `drleedovemagic.net` 於 Cloudflare 註冊，$11.86/年，自動續約已開啟
- 到期日 2027-09-20
- DNS 指向 GitHub Pages，HTTPS 已啟用
- 舊網址 `sherlocklee0212.github.io/drleedovemagic` 會自動轉到新網域

**搜尋收錄**
- Google Search Console 已驗證（網域類型，用 TXT 記錄）
- Sitemap 已提交
- 首頁已送出優先索引請求

### 進行中

- **文章還是空的。** 原本有六篇示範文章，因為內容是虛構的，已全部清空。
  現在六個分類都是 0 篇，等第一篇真實紀錄。

---

## 已經做過的決定

| 決定 | 理由 |
|---|---|
| 站名用「李醫師的鳩是魔法」 | 「鳩」唸起來是「就」，一句話同時帶到品種、醫師身分、魔術 |
| 分類全部用「鳩」字開頭 | 語感統一，讀者一眼看懂網站結構 |
| 用 Jekyll 而不是現成的架站平台 | 寫 Markdown 就能發文，不必付月費，內容永遠是自己的 |
| 網域選 `.net` 而非 `.com` | `.com` 也可註冊，但選定 `.net` |
| **DNS 用灰雲（DNS only），不開 Cloudflare 橘雲代理** | 開橘雲會讓 GitHub 簽不出 HTTPS 憑證，或造成轉址無限迴圈。要開之前必須先把 Cloudflare 的 SSL 模式設成 Full |
| 站徽圖示改成深紫底 | 原圖是淺紫底白鴿，縮到 16px 幾乎看不見 |
| 首屏主視覺用深色 | 舞台照是純黑背景，深色底才能讓照片無縫融入版面 |
| 關於頁註明「醫師專業在人不在鳥」 | 健康類文章是經驗分享，不是獸醫診斷建議 |

---

## 下一步待辦

### 優先

1. **寫第一篇真實文章。** 複製 `_drafts/文章範本.md` 到 `_posts/`，
   檔名格式 `年-月-日-英文代號.md`（例：`2026-10-05-first-flight.md`）。
2. **核對 `about.md` 的自述。** 那段文字是 Claude 代筆的，
   要掛在你名字下，請自己讀一次確認語氣像不像你。

### 之後再說

- 過幾天到 Search Console 看 sitemap 狀態是否從「無法擷取」變成「成功」
- 想換首頁主視覺照片時，做成 1800px 與 960px 兩個尺寸蓋掉現有檔案
- 若之後想用 Cloudflare 的快取與流量統計，再開橘雲並把 SSL 設成 Full
  （不要自己單獨開，設定錯會讓網站轉址迴圈）

---

## 需要記得的事

- **不要刪掉 Cloudflare 上那筆 Google 驗證用的 TXT 記錄**，刪掉會失去 Search Console 驗證。
- **這個 repo 必須維持公開**。GitHub 免費方案規定：private repo 無法發佈 GitHub Pages。
  改成私人的話網站會立刻離線。
- 不想上傳的檔案放進 `private/` 資料夾，那個資料夾已經被 `.gitignore` 排除。
